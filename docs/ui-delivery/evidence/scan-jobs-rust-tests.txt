归影 Phase 1 桌面任务、历史授权与单实例锁验证
时间：2026-08-11T17:49:00+08:00
环境：rustc/cargo 1.92.0; macOS arm64

命令：
1. cargo +1.92.0 fmt --manifest-path src-tauri/Cargo.toml --all -- --check
2. cargo +1.92.0 clippy --locked --manifest-path src-tauri/Cargo.toml --all-targets --all-features -- -D warnings
3. cargo +1.92.0 test --locked --manifest-path src-tauri/Cargo.toml --all-targets --all-features
4. RUSTDOCFLAGS=-D warnings cargo +1.92.0 doc --locked --manifest-path src-tauri/Cargo.toml --no-deps

退出码：全部为 0。

结果：53/53 tests passed，fmt、strict clippy 与 rustdoc 均通过，0 failed。

覆盖：原生一次性 root token、单活动任务、合作式取消与回执解耦；历史 catalog、completed/exact/coverage
资格门、持久 EvidenceReader、窗口绑定 result token、owner/TTL/generation/DB-scope 二次门、pending/in-flight
硬上限、关闭窗口 tombstone、迟到结果丢弃；应用数据库进程锁先于 Store 打开，路径替换、第二实例和子进程
崩溃释放均 fail closed。所有证据读取只接受 resultReadToken；jobId 仅用于当前进程控制。
