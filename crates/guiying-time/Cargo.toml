[package]
name = "guiying-time"
version = "0.1.0"
edition = "2021"
rust-version = "1.77.2"
description = "Pure timestamp normalization and conflict policy for Guiying"
license = "MIT"
publish = false
repository = "https://github.com/siguago/guiying"

[dependencies]
guiying-metadata = { version = "0.1.0", path = "../guiying-metadata" }
